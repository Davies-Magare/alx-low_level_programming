#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @n: Integer to find absolute value
 *
 * Return: Always 0;
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
