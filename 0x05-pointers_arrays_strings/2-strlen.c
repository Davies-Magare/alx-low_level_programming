#include "main.h"

/**
 * _strlen - returns the length of a string.
 * @s: The string whose length is to be determined
 *
 * Return: 0
 */

int _strlen(char *s)
{
	int i;
	int *s[100];

	for (i = 0; s[i] != '\0'; i++)
	{
		_putchar[i];
	}
}
